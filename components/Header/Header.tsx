import React from "react";
import { Typography } from "../Typography";

const Header = () => {
  return (
    <div className="">
      <Typography variant="title">stride</Typography>
    </div>
  );
};

export default Header;
